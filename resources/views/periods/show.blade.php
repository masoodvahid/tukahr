@extends('layouts.app',['heading'=>$period->title,'subheading'=>'مدیریت ستون‌های حقوق و فهرست پرسنل'])@section('content')<div class="grid gap-6 lg:grid-cols-2"><div class="card p-5"><h2 class="font-bold">بارگذاری پرسنل</h2><p class="mt-2 text-sm leading-7 text-neutral-500">فایل باید ستون‌های «نام»، «نام خانوادگی»، «شماره پرسنلی»، «کد ملی» و «پروژه» داشته باشد.</p><form class="mt-4 flex gap-3" method="post" action="{{ route('periods.import',$period) }}" enctype="multipart/form-data">@csrf<input class="field" type="file" name="file" accept=".xlsx,.xls,.csv" required><button class="btn-primary shrink-0">بارگذاری</button></form></div><div class="card p-5"><h2 class="font-bold">انتشار</h2><p class="mt-2 text-sm leading-7 text-neutral-500">با انتشار دوره، ساختار ستون‌ها برای پروژه‌ها ثابت می‌شود.</p><form class="mt-4" method="post" action="{{ route('periods.publish',$period) }}">@csrf<button class="btn-primary" {{ $period->structure_locked_at?'disabled':'' }}>انتشار و قفل ساختار</button></form></div></div><div class="card mt-6 p-5"><div class="mb-4 flex items-center justify-between"><h2 class="font-bold">ستون‌های حقوق و دستمزد</h2><span class="badge">{{ $period->columns->count() }} ستون</span></div>@unless($period->structure_locked_at)<form class="mb-5 grid gap-3 md:grid-cols-5" method="post" action="{{ route('periods.columns.store',$period) }}">@csrf<input class="field" name="title" placeholder="عنوان" required><input class="field" name="column_key" dir="ltr" placeholder="overtime_hours" required><select class="field" name="value_type"><option value="number">عدد</option><option value="text">متن</option><option value="date">تاریخ</option><option value="boolean">بله/خیر</option></select><input class="field" name="unit" placeholder="واحد"><button class="btn-primary">افزودن</button></form>@endunless<div class="flex flex-wrap gap-2">@forelse($period->columns as $c)<span class="badge gap-2">{{ $c->title }} · {{ $c->unit ?: $c->value_type }} @unless($period->structure_locked_at)<form class="inline" method="post" action="{{ route('periods.columns.destroy',[$period,$c]) }}">@csrf @method('DELETE')<button class="text-red-500" data-confirm="این ستون حذف شود؟">×</button></form>@endunless</span>@empty<span class="text-sm text-neutral-400">هنوز ستونی تعریف نشده است.</span>@endforelse</div></div>@endsection
