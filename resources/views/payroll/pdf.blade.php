<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:9px;direction:rtl}table{border-collapse:collapse;width:100%}th,td{border:1px solid #bbb;padding:4px}h1{font-size:15px}</style></head><body><h1>{{ $sheet->project_name_snapshot }} — {{ $sheet->period->title }}</h1><p>نسخه نهایی: {{ $sheet->content_revision }}</p><table><thead><tr><th>پرسنلی</th><th>نام</th>@foreach($sheet->period->columns as $c)<th>{{ $c->title }}</th>@endforeach</tr></thead><tbody>@foreach($sheet->rows as $row)<tr><td>{{ $row->personnel_number_snapshot }}</td><td>{{ $row->first_name_snapshot }} {{ $row->last_name_snapshot }}</td>@foreach($sheet->period->columns as $col)@php($cell=$row->cells->firstWhere('payroll_column_id',$col->id))<td>{{ $cell?->{match($col->value_type){'number'=>'number_value','date'=>'date_value','boolean'=>'boolean_value',default=>'text_value'}} }}</td>@endforeach</tr>@endforeach</tbody></table><h2>تأییدها</h2>@foreach($sheet->approvals as $a)<p>{{ $a->stage }} — {{ $a->actor_name_snapshot }} — {{ $a->approved_at }}</p>@endforeach</body></html>
