@extends('layouts.app',['heading'=>'کاربران','subheading'=>'کاربران، نقش‌ها و پروژه‌های دسترسی'])@section('content')<div class="grid gap-6 xl:grid-cols-[400px_1fr]"><form class="card h-fit space-y-4 p-5" method="post" action="{{ route('users.store') }}">@csrf<h2 class="font-bold">کاربر جدید</h2><div><label class="label">نام و نام خانوادگی</label><input class="field" name="name" required></div><div><label class="label">شماره موبایل</label><input class="field" name="mobile" dir="ltr" inputmode="numeric" required></div><div><label class="label">سطح دسترسی</label><select class="field" name="role_code" id="role_code" required>@foreach($roles as $role)<option value="{{ $role->value }}">{{ $role->label() }}</option>@endforeach</select></div><div id="project_wrap"><label class="label">پروژه</label><select class="field" name="project_id"><option value="">انتخاب پروژه</option>@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->name }}</option>@endforeach</select></div><button class="btn-primary w-full">ایجاد کاربر</button></form><div class="table-wrap"><table class="data-table"><thead><tr><th>نام</th><th>موبایل</th><th>نقش</th><th>پروژه</th><th>وضعیت</th></tr></thead><tbody>@foreach($users as $u)<tr><td>{{ $u->name }}</td><td dir="ltr">{{ $u->mobile }}</td><td>{{ $u->activeAssignment?->role_code ?? '—' }}</td><td>{{ $u->activeAssignment?->project?->name ?? '—' }}</td><td><span class="badge">{{ $u->is_active?'فعال':'غیرفعال' }}</span></td></tr>@endforeach</tbody></table></div></div><script>const role=document.getElementById('role_code'),wrap=document.getElementById('project_wrap');function sync(){wrap.hidden=!['project_operator','project_manager'].includes(role.value)}role.addEventListener('change',sync);sync();</script>@endsection
